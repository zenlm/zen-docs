---
title: zen-agent
description: Tool-calling agent (4B)
---

# zen-agent

Tool-calling agent (4B)

## Overview

[Model documentation coming from existing content]

## Quick Start

```python
from transformers import AutoModelForCausalLM, AutoTokenizer

model = AutoModelForCausalLM.from_pretrained("zenlm/zen-agent")
tokenizer = AutoTokenizer.from_pretrained("zenlm/zen-agent")

inputs = tokenizer("Your prompt", return_tensors="pt")
outputs = model.generate(**inputs)
print(tokenizer.decode(outputs[0]))
```

## Resources

- 🤗 [HuggingFace](https://huggingface.co/zenlm/zen-agent)
- 📦 [GitHub](https://github.com/zenlm/zen-agent)
- 📄 [Paper](/papers/zen-agent_whitepaper.pdf)
